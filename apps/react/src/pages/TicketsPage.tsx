import { DashboardLayout } from "layouts";
import { TicketsTable } from "components";

const TicketsPage = () => {
  return (
    <DashboardLayout>
      <TicketsTable />
    </DashboardLayout>
  );
};

export default TicketsPage;
